#pragma once
#include "Mixture/Core/Base.hpp"

namespace Mixture
{
    class ShaderCompiler
    {
    public:
        static Vector<uint8_t> Compile(const std::string& source, const std::string& profile, const std::string& entryPoint)
        {
            return {};
        }
    };
}
